import 'package:flutter/material.dart';

class UserPage extends StatelessWidget{
  //const UserPage({required Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("Users", style: TextStyle(fontSize: 30),)
    );
  }
}